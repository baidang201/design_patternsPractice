工厂模式

解决的问题：
1 提高内聚。1抽出公共部分，好维护  2用户程序员不用知道实际子类的命名，减少记忆负担。
2 创建时，不知道要实例化哪个具体的子类。


功能
1 Factory提供创建接口的封装 （符合里氏替换，用子类替换父类）
2 类的实例化，推迟到子类。

问题：
1 每个产品的实例化，都要工厂类提供一个方法。Factory接口就永远不能封闭。=》可以创建Factory的子类实现多态实现这点
2 也可以通过参数化工厂方法，传递一个参数用于创建哪个具体的Product。可用反射等技术实现。